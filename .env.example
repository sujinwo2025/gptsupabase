# ============================================================================
# BYTRIX API - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env and fill in your actual values
# All values below have defaults in src/config/endpoints.js
# ============================================================================

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
PORT=3000
NODE_ENV=production
LOG_LEVEL=info
DOMAIN=https://file.bytrix.my.id

# ============================================================================
# S3 / FILE STORAGE CONFIGURATION
# ============================================================================
# Supports AWS S3, DigitalOcean Spaces, MinIO, or any S3-compatible service
S3_ENDPOINT=https://s3.example.com
S3_REGION=us-east-1
S3_ACCESS_KEY=your_access_key
S3_SECRET_KEY=your_secret_key
S3_BUCKET=files
# URL expiry time in seconds (default: 3600 = 1 hour)
S3_URL_EXPIRY=3600

# ============================================================================
# SUPABASE / DATABASE CONFIGURATION
# ============================================================================
# Supabase provides PostgreSQL database with built-in auth
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_key
# Table name for storing file metadata
SUPABASE_TABLE_UPLOADS=uploads

# ============================================================================
# CUSTOM GPT API CONFIGURATION
# ============================================================================
# Connect to OpenAI API or custom GPT endpoint
GPT_API_KEY=your_gpt_api_key
GPT_API_URL=https://your-custom-gpt-endpoint
GPT_MODEL=gpt-3.5-turbo
# Temperature for text generation (0.0 - 1.0, lower = more deterministic)
GPT_TEMPERATURE=0.7
# Maximum tokens to generate in response
GPT_MAX_TOKENS=2000

# ============================================================================
# API ENDPOINT PATHS CONFIGURATION (Optional)
# ============================================================================
# Customize endpoint paths without changing code
# Default base path for all API endpoints
API_BASE_PATH=/api/v1
# Base path for file management endpoints
FILE_ENDPOINT_BASE=/api/v1/files
# Base path for GPT text generation endpoints
GPT_ENDPOINT_BASE=/api/v1/gpt
# Base path for GPT CRUD actions (list/get/delete files)
GPT_ACTIONS_BASE=/api/v1/gpt/actions

# ============================================================================
# SECURITY & EXPIRY CONFIGURATION
# ============================================================================
# JWT token expiry time in seconds (default: 3600 = 1 hour)
JWT_EXPIRY=3600
# Signed URL expiry time in seconds (default: 3600 = 1 hour)
SIGNED_URL_EXPIRY=3600
# Maximum file size in bytes (default: 104857600 = 100 MB)
MAX_FILE_SIZE=104857600

# ============================================================================
# NOTES
# ============================================================================
# - All configuration is loaded from environment variables
# - Centralized in src/config/endpoints.js with sensible defaults
# - Change endpoints by modifying API_BASE_PATH, FILE_ENDPOINT_BASE, etc.
# - No code changes needed - just update .env and restart the server
# ============================================================================
